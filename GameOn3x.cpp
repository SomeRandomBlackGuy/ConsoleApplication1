// Game Over
// A first C++ program
#include "stdafx.h"
#include <iostream> // preprocessor for the library stream (Helps display text)
using namespace std;


int main() // Header function returns an integer
{  //Beginning of function 
	cout << "Game On!" << endl;
	//std : name space in the stream library
	//cout : an object that sends output to the library stream
	// ::scope resolution operator (used to prefix a name space)
	// << : output operators sends string to cout
	// "" : String literal
	//end1: predefined in the library ()
	system("pause");
	return 0;
} // Marks the end of the function
